name: ⚡ USB-Link Pro — Build Windows EXE

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch: # Позволяет запускать сборку вручную из вкладки Actions

jobs:
  build-windows:
    name: 🖥️ Build Portable Windows EXE
    runs-on: windows-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm install

      - name: 🔨 Build Portable Windows Executable
        run: npm run build-exe
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 📂 Upload EXE to GitHub Actions Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: USB-Link-Pro-Windows-Portable
          path: dist/*.exe
          retention-days: 30

      - name: 🚀 Create or Update GitHub Release
        if: startsWith(github.ref, 'refs/tags/') || github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || 'v1.0.0-latest' }}
          name: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || 'USB-Link Pro — Актуальная версия' }}
          files: dist/*.exe
          generate_release_notes: true
          prerelease: ${{ !startsWith(github.ref, 'refs/tags/') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
